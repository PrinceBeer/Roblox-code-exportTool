# Roblox .rbxl 代码导出 · 完整流程

> 目标:从 Roblox 场景文件(`.rbxl`)中导出代码(Script / LocalScript / ModuleScript),按层级整理成文件夹,生成 Rojo 项目,在 VS Code 中编辑。
> 本流程基于实际项目(主场景 5 个脚本 + 子场景 67 个脚本,含 Matter ECS 框架)完整跑通的经验编写。

## 流程图

```mermaid
flowchart TD
    A[获得 .rbxl 文件] --> B{工具选型}
    B -->|remodel| X1[✗ 已停维护 0.11.0 是终版<br/>读不了新版格式/LZ4]
    B -->|Rojo| X2[✗ 只读 XML .rbxlx<br/>读不了二进制 .rbxl]
    B -->|Lune| C[✓ 官方继任者<br/>deserializePlace 直接读]
    C --> D[配置 export_scenes.lua<br/>PLACES 或自动扫描]
    D --> E[运行 lune run export_scenes.lua]
    E --> F[输出 export/场景名/<br/>src + default.project.json + 清单]
    F --> G{rojo build 验证}
    G -->|通过| H[VS Code + Rojo 打开编辑]
    G -->|报错| I[对照"故障排查"修 model.json]
    H --> J[同步回 Studio 前<br/>备份 .rbxl + 只同步映射容器]
```

## 1. 背景:为什么 remodel 不行

### 1.1 remodel 已停止维护

- 官方仓库最后提交(2023-07-22)标题就是 *"Deprecate remodel in favor of Lune"*(弃用 remodel,改用 Lune)。
- **v0.11.0 是最终版**,永远不会有新版。任何"下载新版 remodel"的建议都不可行。
- 网上的 gitee 镜像 `gitee.com/mirrors/remodel` 镜像的其实是 **Python 的 RethinkDB 数据库库**(linkyndy/remodel),不是 Roblox 工具——克隆下来全是 `.py` 文件,纯属误导。

### 1.2 新版 .rbxl 格式让 remodel 0.11.0 直接报错

实测两个文件:

```
[WARN ] Unknown value type ID 0x21 (33) ... AccessoryDescription.Capabilities
[WARN ] Unknown value type ID 0x1f (31) ... AccessoryDescription.HistoryId
[ERROR] ... caused by: Type mismatch: Property Animation.Tags should be SharedString, but it was Tags
```

```
[ERROR] ... caused by: Decompression failed. Input invalid or too long?
```

原因:

| 现象 | 原因 |
|---|---|
| `Unknown value type ID 0x21/0x1f` | 新版 Studio 引入的新属性类型:Capabilities(0x21=33)、SharedStringBuffer(0x1f=31),remodel 不认识 |
| `Animation.Tags should be SharedString, but it was Tags` | 文件里 Tags 类型与 remodel 内置属性表(旧的)不一致,反序列化直接失败 |
| `Decompression failed` | 文件是 **LZ4 压缩**(新版 Studio 默认),remodel 只支持 zlib/未压缩 |

诊断方法:读文件头第 12-15 字节(小端 u32),`1` = zlib 压缩,`2` = LZ4 压缩。

### 1.3 Rojo 也读不了二进制 .rbxl

`rojo build` 引用 `.rbxl` 会报:

```
Rojo project referred to a file using $path that could not be turned into a Roblox Instance by Rojo.
```

Rojo 只支持 XML 格式(`.rbxlx` / `.rbxmx`)。

## 2. 工具选型:Lune(remodel 官方继任者)

- 项目:[lune-org/lune](https://github.com/lune-org/lune),Rust 编写,持续活跃维护。
- 内置 `@lune/roblox` 库:`roblox.deserializePlace(文件内容)` 直接得到 DataModel 实例,**支持新版属性类型 + LZ4 压缩**。
- 实例 API 与 Roblox 一致:`GetChildren()` / `GetDescendants()` / `GetService()` / `ClassName` / `Name` / `Source` / 直接属性访问。
- 官方文档还有专门的 [Remodel Migration](https://lune-org.github.io/docs/roblox/3-remodel-migration) 章节。

### 安装(Windows)

1. 从 [GitHub Releases](https://github.com/lune-org/lune/releases) 下载 `lune-<版本>-windows-x86_64.zip`(约 5 MB)。
   - 国内网络直连慢:用镜像前缀,如 `https://ghproxy.net/https://github.com/lune-org/lune/releases/download/v0.10.5/lune-0.10.5-windows-x86_64.zip`
2. 解压得到 `lune.exe`,放到 PATH 目录(如 `E:\Tool\`)。
3. 验证:`lune --version`。
4. 也可以用 aftman:`aftman.toml` 中加 `lune = "lune-org/lune@0.10.5"`。

## 3. 导出脚本 export_scenes.lua

脚本功能:

- 读取指定(或当前目录全部)`.rbxl`;
- 导出所有脚本,保持实例层级:
  - `Script` → `.server.lua`、`LocalScript` → `.client.lua`、`ModuleScript` → `.lua`
  - **脚本自身带子脚本** → 按 Rojo 惯例导出成 `文件夹/init.<ext>` + 子文件
- **轻量容器**保留类名:`ScreenGui / Frame / TextButton / TextLabel / RemoteEvent / RemoteFunction / BindableEvent / Model` 等 → 生成 `<name>.model.json`(className + 关键属性);
- **重量级内容**(Part / MeshPart / Humanoid / 人形模型等)→ 只导出其中的脚本作参考,不写入项目映射;
- 自动生成**同步安全**的 `default.project.json`;
- 生成 `导出清单.txt`(UTF-8,含脚本总数与预期校验)。

### 配置

方式一:手动指定(推荐)。

```lua
local PLACES = {
	{ file = "MyPlace.rbxl", name = "MyPlace", label = "备注" },
}
```

方式二:自动扫描——把 `PLACES` 清空成 `PLACES = {}`,运行后导出当前目录所有 `.rbxl`;用 `SKIP_FILES` 排除不需要的文件。

### 运行

```
lune run export_scenes.lua
```

在工作目录(脚本与 .rbxl 所在目录)下运行。输出:

```
export/MyPlace/
├── src/                    # 按实例层级镜像的代码
│   ├── ServerScriptService/
│   ├── ReplicatedStorage/
│   └── ...                 # 含 .lua 与 .model.json
├── default.project.json    # Rojo 项目配置
└── 导出清单.txt
```

## 4. 验证

```
cd export/MyPlace
rojo build default.project.json -o verify.rbxlx
```

构建通过 = 项目结构、model.json 属性编码全部合法。可进一步检查产物里包含/不包含预期内容(如确认模型零件没有混入)。

## 5. VS Code + Rojo 使用

1. VS Code 打开 `export/MyPlace` 文件夹;
2. 安装 Rojo 扩展,命令面板 **Rojo: Open menu** → serve this project;
3. Studio 打开对应 `.rbxl`,点 Rojo 插件 **Connect**,代码即同步进 Studio。

> 同一时间只能跑一个 Rojo 服务器、服务一个场景;换场景需重启服务器。

## 6. 同步回 Studio 的注意事项(重要)

- **Rojo 同步会删除"已映射容器里项目中没有的内容"**,且不可恢复 → **同步前务必备份 .rbxl**。
- 项目只映射**能完整回同步**的容器(纯代码服务,如 ReplicatedStorage / ServerScriptService / StarterPlayer)。
- **不要映射**含以下内容的容器,否则同步会删掉它们:
  - UI(StarterGui):`Position`/`Size` 等 UDim2 布局属性无法用 model.json 表示;
  - 零件/人形模型(如 ServerStorage 里的 R15 模型):零件、约束、贴图无法导出。
- 这些容器里的脚本照常导出,改完后手动复制回 Studio,或自行把对应子树加进 project.json(清楚后果的前提下)。

## 7. 已知限制

| 限制 | 原因 |
|---|---|
| UI 的 `Position`/`Size`/`CornerRadius` 等 UDim2/UDim 属性不导出 | Rojo 7.7 的 `AmbiguousValue` 没有对象变体,`{"scale":...}` 直接解析失败(见故障排查 #2) |
| `Font` 属性不导出 | Lune 读 Font 属性报 "malformed property info";同步后用默认字体 |
| 脚本自身属性(如 `Disabled`)不导出 | model.json 只写容器属性 |
| 脚本下的非脚本子级(如 Animation、StringValue)不导出 | 只镜像脚本与轻量容器 |
| 重量级内容只导出脚本 | 零件/网格/人形无法用文本表示 |

## 8. 故障排查

| 报错/现象 | 原因 | 解决 |
|---|---|---|
| `Type mismatch: Property Animation.Tags should be SharedString` | remodel 太老,不认识新属性类型 | 用 Lune,不要用 remodel |
| `Decompression failed. Input invalid or too long?` | 文件是 LZ4 压缩 | 用 Lune(支持 LZ4) |
| `could not be turned into a Roblox Instance by Rojo` | Rojo 只读 XML,不读二进制 .rbxl | 用本工具导出,或先在 Studio 里另存为 .rbxlx |
| `data did not match any variant of untagged enum UnresolvedValue` | model.json 里写了 Rojo 不支持的属性值(如 UDim2 对象) | 去掉该属性(脚本已自动跳过 UDim2/UDim) |
| 导出脚本数量与预期不符,且少的部分集中在一个文件夹 | Lune 的 `GetChildren()` 表有空洞,`ipairs` 提前停止 | 遍历一律用 `pairs()`,不要用 `ipairs()` |
| `fs.remove` / `fs.exists` 报 "attempt to call a nil value" | Lune 0.10.x 没有这两个 API | 用 `fs.removeDir` / `fs.readDir` |
| 中文文件被改坏成乱码 | Windows PowerShell 5.1 的 `Get-Content` 默认按 GBK 解码,写回 UTF-8 时损坏中文 | 用 VS Code 或 AI 工具改文件;PowerShell 里用 `[System.IO.File]::ReadAllText(path, [Text.Encoding]::UTF8)` |

## 9. Lune API 踩坑记录

- `GetChildren()` / `GetDescendants()` 返回的表**可能有空洞**(实测 42 个脚本的文件夹只迭代出 1 个)——必须 `pairs()` 遍历;
- `fs` 库:`readFile / writeFile / writeDir / readDir / removeDir` 存在;`remove / exists` **不存在**;
- 实例属性直接访问:`script.Source`、`gui.Position`;枚举是 userdata,`tostring()` 得 `Enum.TextXAlignment.Center`,取最后一段即为枚举名;
- `GetProperties()` 不存在;`Font` 属性读取报 "malformed property info",跳过即可;
- `roblox.deserializePlace(fs.readFile(path))` 是标准读法,官方文档示例即此。

## 10. 扩展:导出 .rbxmx 模型文件

思路与 .rbxl 相同:Lune 提供 `roblox.deserializeModel`(模型文件,不含 DataModel 服务)。把脚本里 `deserializePlace` 换成 `deserializeModel`、去掉服务遍历即可,其余逻辑(脚本/容器/映射)通用。
